// Write a loop that makes seven calls to console.log to output the a pyramid of left aligned triangles
let pyramid = '';

for (let i = 0; i < 7; i++) {
  pyramid = pyramid + '#';
  console.log([pyramid]);
}
